# Runtime story data

This directory contains checked-in story packages consumed by the application. For the authoritative distinction between Markdown authoring inputs, generated JSON, runtime inputs, regeneration commands, and files contributors must not edit directly, see [Content ownership and reproducibility](../../docs/CONTENT_OWNERSHIP.md).

Run `npm run content:validate:runtime` after any change beneath this directory.
